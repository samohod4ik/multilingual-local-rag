# Provenance

Application code in this repository was written for this package. No other tree was copied into it.

Unicode BM25, immutable snapshot publication, and a content-addressed vector cache are ordinary retrieval ideas. They are implemented here against `SourceDocument`. Model weights are not included.
