# Third-party notices

Runtime code in this version uses the Python standard library only.

Development tools (`pytest`, `ruff`, `mypy`, `build`, `setuptools`, `wheel`) are pinned with hashes in `requirements-dev.lock`. Their licenses are those of the upstream projects and are not relicensed by this repository.
