# Contributing

Open a pull request against `main`. Keep public docs, comments, and metadata in English. Benchmark document and query strings may be Russian or English when a fixture requires both.

Run the checks listed in `README.md` with the repository virtualenv on Python 3.12 before asking for review.

Do not add host paths, credentials, indexes, or model weights.
